The two analysis codes look for directories with the data that must be in the same directory as the analysis codes.

 IPA_Analysis_part_I.py 
>python3  IPA_Analysis_part_I.py 
This code goes through each subdirectory (P%,BS) that contains 100 runs and calculates the IPA for each run.
#This code calculates the IPA for each run for each batch size for each pruning percentage for each pruning layer.
#A single file is created for each batch size directory. The file contains the IPA for each run and at the bottom lists the IPA_avg and STD for the runs in that directory.
#A separate code ( IPA_Analysis_part_II.py) finds the highest IPA_avg among the different batch_sizes. That is the IPA that will be plotted for that pruning percentage and pruning layer combo.
#Line 45 can be modified depending on the number of runs available.


 IPA_analysis_part_II.py
>python3  IPA_analysis_part_II.py
#This code uses the output files generated by IPA_Analysis_code_part_I.py.
#This code produces two  text files that are located within the directory for each type of pruning layer:
 -One file lists the highest IPA_avg and its STD among the different batch sizes for each pruning percentage and pruning layer combo for plotting.
 -The second file lists the IPA-avg and STD for each BS for each P%.


